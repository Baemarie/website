# Mboa Insider assets

The logo image is intentionally not committed here to avoid binary-file pull issues.

Add the logo back at this exact path when uploading assets to GitHub:

```txt
assets/mboa-insider-logo.jpg
```

All pages already reference that path, so the branding will work as soon as the image is restored.
