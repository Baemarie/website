const menuBtn=document.querySelector('.menu-btn');const nav=document.querySelector('nav');if(menuBtn)menuBtn.addEventListener('click',()=>nav.classList.toggle('open'));
document.querySelectorAll('.theme').forEach(b=>b.addEventListener('click',()=>{document.documentElement.classList.toggle('dark');localStorage.setItem('mboa-theme',document.documentElement.classList.contains('dark')?'dark':'light')}));if(localStorage.getItem('mboa-theme')==='dark')document.documentElement.classList.add('dark');
const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
const countIO=new IntersectionObserver(entries=>entries.forEach(e=>{if(!e.isIntersecting)return;const el=e.target,target=parseFloat(el.dataset.target);let v=0;function step(){v+=target/52;if(v>=target){el.textContent=target%1?target.toFixed(1):Math.round(target);return}el.textContent=target%1?v.toFixed(1):Math.round(v);requestAnimationFrame(step)}step();countIO.unobserve(el)}));document.querySelectorAll('.counter').forEach(el=>countIO.observe(el));
document.querySelectorAll('.filters button').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.filters button').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;document.querySelectorAll('.dest-card').forEach(card=>{card.style.display=(f==='all'||card.dataset.cat.includes(f))?'block':'none'})}));
